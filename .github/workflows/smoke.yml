name: Smoke Test

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare env
        shell: pwsh
        run: ./scripts/bootstrap-env.ps1 -Quiet

      - name: Compose config
        run: docker compose config

      - name: Compose config (mounts)
        run: docker compose -f docker-compose.yml -f docker-compose.mounts.yml config

      - name: Compose config (xdebug)
        run: docker compose -f docker-compose.yml -f docker-compose.xdebug.yml config

      - name: Compose config (proxy)
        run: docker compose -f docker-compose.proxy.yml config

      - name: Boot stack
        shell: pwsh
        run: ./scripts/up.ps1 -WithProxy:$false -WithTools -WithMail

      - name: Wait for site
        shell: pwsh
        run: |
          $port = (Select-String -Path .env -Pattern '^WORDPRESS_PUBLISHED_PORT=').Line.Split('=')[1]
          for ($i = 0; $i -lt 30; $i++) {
            try {
              $status = (Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port").StatusCode
              if ($status -eq 200) { exit 0 }
            }
            catch {}
            Start-Sleep -Seconds 2
          }
          throw "WordPress did not become ready in time."

      - name: Wait for adminer
        shell: pwsh
        run: |
          $port = (Select-String -Path .env -Pattern '^ADMINER_PUBLISHED_PORT=').Line.Split('=')[1]
          for ($i = 0; $i -lt 30; $i++) {
            try {
              $status = (Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port").StatusCode
              if ($status -eq 200) { exit 0 }
            }
            catch {}
            Start-Sleep -Seconds 2
          }
          throw "Adminer did not become ready in time."

      - name: Wait for mailpit
        shell: pwsh
        run: |
          $port = (Select-String -Path .env -Pattern '^MAILPIT_PUBLISHED_PORT=').Line.Split('=')[1]
          for ($i = 0; $i -lt 30; $i++) {
            try {
              $status = (Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port").StatusCode
              if ($status -eq 200) { exit 0 }
            }
            catch {}
            Start-Sleep -Seconds 2
          }
          throw "Mailpit did not become ready in time."

      - name: WP-CLI check
        shell: pwsh
        run: ./scripts/wp.ps1 core version

      - name: Shutdown
        if: always()
        shell: pwsh
        run: ./scripts/down.ps1 -Volumes
